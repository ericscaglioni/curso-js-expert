const validCarCategory = require('./valid-car-category.json')
const validCar = require('./valid-car.json')
const validCustomer = require('./valid-customer.json')

module.exports = {
  validCarCategory,
  validCar,
  validCustomer
}
